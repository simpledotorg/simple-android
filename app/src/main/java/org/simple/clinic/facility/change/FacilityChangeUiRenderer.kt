package org.simple.clinic.facility.change

import org.simple.clinic.mobius.ViewRenderer

class FacilityChangeUiRenderer(private val ui: FacilityChangeUi) : ViewRenderer<FacilityChangeModel> {

  override fun render(model: FacilityChangeModel) {
    // Nothing to do here yet
  }
}
