include ':app', ':router', ':mobius-migration', ':mobius-base', ':simple-platform', ':simple-visuals', ':threetenbp-extra'

buildCache {
    local(DirectoryBuildCache) {
        directory = new File(rootDir, 'build-cache')
        removeUnusedEntriesAfterDays = 30
    }
}
