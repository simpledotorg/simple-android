package org.simple.clinic.search.results

import org.simple.clinic.mobius.ViewRenderer

class PatientSearchResultsUiRenderer(
    private val ui: PatientSearchResultsUi
) : ViewRenderer<PatientSearchResultsModel> {

  override fun render(model: PatientSearchResultsModel) {
    /* Nothing to do here. Yet. */
  }
}
